name: hello
on:
    push:
        branches: [main]

jobs:
    greet:
      runs-on: ubuntu-latest
      steps:
       - name: code checkout
         uses: actions/checkout@v4

       - name: greeting hello
         run: echo "hello from github actions!"