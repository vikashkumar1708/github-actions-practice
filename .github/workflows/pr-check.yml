name: pr-check
on:
  pull_request:
      branches: [main]

jobs:
    pr_check:
        runs-on: ubuntu-latest
        steps:
          - name: branch name
            run: |
              echo "PR check running for branch: ${{ github.head_ref }}"
